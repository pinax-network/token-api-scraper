# bunfig.toml

# Runs before `bun ./file.ts` and `bun run <script>`
preload = ["./lib/proto-polyfill.ts"]

[test]
# Preload the proto polyfill before running tests to fix tronweb protobuf issues
preload = ["./lib/proto-polyfill.ts"]
